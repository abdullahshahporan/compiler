// Test: Array misuse detection
// Expected: Semantic errors for using array without subscript
//           and using non-array with subscript

purno main() shuru
    array purno arr[3];
    arr[0] = 10;
    arr[1] = 20;

    // Error: using array name without subscript
    purno x = arr;

    // Error: using non-array with subscript
    purno y = 5;
    purno z = y[0];

    ferot 0;
shesh
