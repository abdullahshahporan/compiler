// Test: Division by zero (runtime error)
// This passes semantic analysis but fails at runtime

purno main() shuru
    purno a = 10;
    purno b = 0;
    purno c = a vag b;
    dekhao(c);
    ferot 0;
shesh
